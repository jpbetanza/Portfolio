const AppContainer =({children})=>(
        <div className="bg-gradient-to-r from-sky-800 to-indigo-600 h-screen flex justify-between items-center duration-300">{children} </div>
)

export default AppContainer